import { createAction } from "redux-actions";
export const showNextPhoto = createAction("SHOW_NEXT_PHOTO");
export const showPrevPhoto = createAction("SHOW_PREV_PHOTO");
export const showPhoto = createAction("SHOW_PHOTO");
